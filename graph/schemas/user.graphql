# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type User {
  id: ID!
  name: String!
}

type UserQuery {
  users: [User!]!
}

input NewUser {
  name: String!
}

type UserMutation {
  createReceipt(input: NewUser!): User!
}
